import React from 'react'
// const getChar = async (char) => {
//   const response = await fetch(char)
//   const data = response.json()
//   const img = data.image
//   console.log(img)
//   return img
// }
const EpisodeCharacters = ({ reqPath }) => {
  console.log(reqPath)
  // reqPath.forEach((i) => {
  //   console.log(reqPath[i])
  // })

  // const getImg = async () => {
  //   console.log(reqPath)
  //   const response = await fetch(reqPath)
  //   const data = await response.json()
  //   return data.image
  // }
  // const img = getImg()
  // console.log(img)
  // return <img className='episodeCharacters' src={img}></img>
}

export default EpisodeCharacters
